<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/bg_screen"
    tools:context=".presentation.home.HomeFragment">

    <androidx.appcompat.widget.AppCompatImageButton
        android:id="@+id/btnProfile"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/ic_profile"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginEnd="4dp"
        android:layout_marginTop="8dp"/>

    <FrameLayout
        android:id="@+id/searchExpandContainer"
        android:layout_width="48dp"
        android:layout_height="48dp"
        app:layout_constraintTop_toTopOf="@id/btnProfile"
        app:layout_constraintBottom_toBottomOf="@id/btnProfile"
        app:layout_constraintEnd_toStartOf="@id/btnProfile"
        android:layout_marginEnd="13dp">

        <View
            android:id="@+id/searchBackground"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone"/>

        <EditText
            android:id="@+id/searchEditText"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@null"
            android:hint="Search..."
            android:imeOptions="actionSearch"
            android:inputType="text"
            android:paddingStart="16dp"
            android:paddingEnd="56dp"
            android:textColor="@color/white"
            android:textColorHint="#80FFFFFF"
            android:visibility="gone"/>

    </FrameLayout>

    <androidx.appcompat.widget.AppCompatImageButton
        android:id="@+id/btnSearch"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/ic_search"
        app:layout_constraintTop_toTopOf="@id/searchExpandContainer"
        app:layout_constraintBottom_toBottomOf="@id/searchExpandContainer"
        app:layout_constraintEnd_toEndOf="@id/searchExpandContainer"/>

</androidx.constraintlayout.widget.ConstraintLayout>